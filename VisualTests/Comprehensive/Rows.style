@import "./Base.style"

root{
	align-items: center;
}

rounds {
	flex-axis: column;
	align-items: center;
}

header, row {
	flex-axis: row;
	align-items: center;
}

row > *, header > *{
	width: 50px;
	height: 50px;
	align-items: center;
	justify-content: center;
	border: 1px solid black;
}

